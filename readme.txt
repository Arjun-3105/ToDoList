This is a GUI based to-do-list application, it can store name of the task, due date, its priority status, its on going status and a unique task ID. User can add tasks, remove tasks, see their tasks,  as well as change the on going status of the task when it is done


Todolist contains the schema commands that can be used to create the required database in user's machine--this makes the code compatbile with all machines
